extends layout

block content
	h1= title
	p Welcome to #{title}
	form(name="login", method="post")
		hr
		div.input
				span.label Sign in with the passphrase 
				input(type="password", name="password")
				span  
				input(type="submit", value="Sign in")
